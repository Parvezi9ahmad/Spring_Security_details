server:
  port: 8083
spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/ecommerces?useSSL=false
    username: root
  jpa:
    hibernate:
      ddl-auto: update     
    show-sql: true
    
    
  batch:
    jdbc:
      initialize-schema: ALWAYS